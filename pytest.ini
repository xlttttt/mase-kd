# pytest.ini
[pytest]
python_files = test_* *_tb.py
log_file = pytest.log
markers =
    integration: integration tests that may download data or require longer runtime
    large: large tests requiring significant compute (skipped in CI)